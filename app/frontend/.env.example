# Base URL of the backend API. Read at build time by Vite.
# For local dev, leave unset (falls back to http://localhost:8000).
# For Railway, set this to the backend service's public URL,
# e.g. https://<backend-service>.up.railway.app
VITE_API_URL=http://localhost:8000
